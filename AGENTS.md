# 项目开发规范与 AI 协作准则 (AGENTS.md)

本项目为 **ESP32 物联网实战闯关项目（esp32-starter-journey）**。所有参与本项目的 AI 助理与开发者均需遵循以下协作准则与工程规范。

---

## 📌 一、 核心硬件与环境规范

* **主控芯片**：ESP32-D0WD-V3 / ESP32-WROOM-32E
* **存储配置**：8 MB SPI Flash (`dio` 40MHz) + 2 MB Quad SPI PSRAM
* **开发框架**：ESP-IDF v6.0.2 / FreeRTOS / LVGL v9
* **显示与触摸**：1.69 寸 ST7789 SPI LCD (240×280) + CST816S I2C 电容触摸
* **关键引脚速查**：
  * 板载 LED2：`GPIO27` (高电平点亮)
  * 用户按键 SW3：`GPIO39 (VN)` (输入专用，低电平有效)
  * 屏幕 SPI：CS(`GPIO5`), DC(`GPIO17`), SCLK(`GPIO18`), MOSI(`GPIO19`), RST(`GPIO21`), 背光(`GPIO26`)
  * 触摸 I2C：SCL(`GPIO22`), SDA(`GPIO23`), INT(`GPIO35`)
  * 传感器：DHT11(`GPIO25`), HC-SR04(`GPIO32/33`), SR602(`GPIO34`), NTC(`GPIO36/VP`), WS2812(`GPIO26`)
* **硬件约束与屏幕固定配置**：
  * **屏幕排线与视口配置（固定铁律）**：
    `ESP_ERROR_CHECK(esp_lcd_panel_set_gap(s_panel, 0, 20));`
    `ESP_ERROR_CHECK(esp_lcd_panel_mirror(s_panel, false, false));`
    在 LVGL 端口中：`.rotation = { .swap_xy = false, .mirror_x = false, .mirror_y = false }`，绝不可修改为其他值，否则会导致文字镜像反转或花屏！
  * `GPIO26` 与 LCD 背光复用，调试 WS2812 必须拔下 JP7 跳线帽；
  * `GPIO34/35/36/39` 为纯输入管脚，切勿配置为输出。

---

## 🎯 二、 关卡开发与交付流程规范

项目采用 **“主干持续演进 + 语义化 Commit 提交”** 模式：

```
[编写关卡源码与 code/ 归档] ───► [本地编译验证] ───► [学习计划打卡] ───► [Commit + Push]
```

### 1. 关卡代码组织
* `code/` 目录存放各关卡独立、完整的实验源码（如 `code/07_adc_ultrasonic/01_adc_raw.c` 等）。
* `main/app_main.c` 承载当前关卡/当前调试实验的完整可运行代码。
* 代码必须保持极高可读性，包含：关卡名称、学习目标、核心概念注释与关键参数说明。

### 2. 关卡完成交付标准（三步走）
每次打通一个关卡并验证无误后，依次执行：

1. **更新打卡清单**：
   在根目录 [`ESP32_小白入门实战学习计划.md`](./ESP32_小白入门实战学习计划.md) 中将对应关卡标记为 `[x]`。
2. **归档源码与提供切换**：
   确保相关实验已同步至 `code/` 目录，并在教程文档中附上源码文件链接。
3. **提交 Git Commit 并同步至 GitHub**（⚠️ **后续不再打 Tag**）：
   采用标准语义化提交信息：
   ```bash
   git add .
   git commit -m "feat(level-<N>): <关卡名称与简短说明>"
   git push origin main
   ```

---

## 💻 三、 编码与构建规范

1. **日志与调试**：
   * 必须使用 `ESP_LOGI` / `ESP_LOGW` / `ESP_LOGE` 宏代替裸 `printf`（除特殊格式化输出外），并为模块定义清晰的 `TAG`。
2. **延时与任务调度**：
   * 必须使用 FreeRTOS 标准延时 `vTaskDelay(pdMS_TO_TICKS(ms))`，禁止使用空循环死等。
3. **内存管理**：
   * 大块显存、图像 Buffer 或 LVGL 绘制缓存优先使用 `heap_caps_malloc(size, MALLOC_CAP_SPIRAM)` 分配至 2MB PSRAM 中。
4. **编译构建命令**：
   * 编译：`idf.py build`
   * 烧录：`idf.py -p <PORT> flash`
   * 串口监视：`idf.py -p <PORT> monitor`

---

## 🎨 四、 关卡专属封面插画生成规范

为保证开源电子书教程（`book/`）的出版级视觉品质与系列一致性，后续每个关卡生成封面海报时均需严格遵循以下规范：

### 1. 文件命名与存放路径
* **文件路径**：`docs/images/esp32_level<N>_cover.jpg`（如 `docs/images/esp32_level5_cover.jpg`）
* **图片比例**：固定为 **`16:9`** 横屏高清构图。

### 2. 文字排版与视觉规范（保持系列统一）
* **参考底图对齐**：调用生成工具时，需传入已有封面（如 `docs/images/esp32_level3_cover.jpg`）作为参考图（`ImagePaths`），确保顶部排版、字体字号和荧光青色调一致；
* **顶部主标题**：统一格式为 `LEVEL <NN>: <中文关卡名称>`（例如 `LEVEL 05: ST7789 彩屏驱动与图形渲染`），发光 3D 科技字体居中；
* **副标题格式**：**必须为中文核心概念提炼（禁止使用纯英文副标）**，例如 `硬件 SPI DMA 显存加速与几何渲染`，字号略小居中，带有优雅光效分割线。

### 3. 背景场景与意境设计（关关不同，紧扣原理）
* **拒绝千篇一律**：禁止使用单调的面包板插线摆拍；
* **深层意境融合**：必须将当前章节的核心物理机制具象化呈现：
  * *例如 PWM*：展现平滑脉宽调制光晕与呼吸色彩；
  * *例如外部中断*：展现机械微动按键触发电光穿透打断 CPU 核心与全息下降沿示波器波形；
  * *例如 ADC / 传感器*：展现连续模拟正弦波、微观温度能量场与超声波测距声波光束；
  * *例如 LVGL / 屏幕*：展现悬浮的 1.69寸彩屏、全息玻璃拟物控件与手指触摸水波纹。
* **艺术基调**：深色硬核芯片微观视角、电影级景深、环境光粒子与发光电路走线。

---

## 💡 五、 概念讲解与教程沉淀准则 (小白友好原则)

为确保本项目教程对零基础嵌入式初学者具备极致的友好度与启发性，在解答与讲解技术概念时必须严格遵循以下三项铁律：

### 1. 通俗易懂，生活化比喻（秒懂设计）
* **拒绝无趣术语堆砌**：讲解任何硬件或底层原理时，必须优先引入生活中的具象化场景（如尺子刻度、天平砝码、菜市场噪音、水龙头水流等）打比方；
* **视觉化 ASCII 图解**：搭配生动直观的文本字符图，降低认知负荷。

### 2. 由浅入深，三层阶梯进阶
* **第一层（直观感受）**：它在物理世界上是什么感觉？解决了什么生活问题？
* **第二层（底层原理与设计权衡）**：为什么芯片设计师要这样设计？背后的成本、物理极限和约束是什么？（为什么不大也不小？）
* **第三层（行业全景与选型法则）**：真实工业界如何划分段位？各行各业在什么场景选什么器件？

### 3. 即时沉淀，Markdown 就地回填
* 每当用户提出概念疑问或要求展开剖析时，讲解完成后**必须主动将通俗易懂的讲解内容，精准回填/沉淀到对应 Markdown 教程（`book/`）的合适小节中**，让整套开源教程持续生长。

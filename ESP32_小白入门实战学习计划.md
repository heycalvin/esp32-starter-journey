# ESP32 物联网开发板 · 小白零基础实战学习计划

> **学习原则**：由浅入深、单点突破、步步有反馈。每完成一个关卡，在打卡框中勾选 `[x]`，遇到任何疑问或报错随时呼叫 AI 协助排查。

---

## 📌 开发板硬件速查表（引脚与资源）

在编写代码时，需要用到的引脚对照如下：

| 功能模块 | 引脚 / 资源 | 模式 / 特性 | 硬件与使用注意事项 |
| :--- | :--- | :--- | :--- |
| **可编程 LED2 (蓝)** | **GPIO27** | 输出 (Output) | 输出高电平 `1` 点亮，低电平 `0` 熄灭 |
| **用户按键 SW3** | **GPIO39 (VN)** | 输入 (Input Only) | 仅作输入；松开为高电平 `1`，按下为低电平 `0` |
| **ST7789 屏幕** | **GPIO5(CS), 17(DC), 18(SCLK), 19(MOSI), 21(RST)** | SPI 总线 | 1.69 英寸彩屏，分辨率 240 × 280 |
| **屏幕背光 (BL)** | **GPIO26** | 输出 (PWM/开关) | 经 JP7 跳线帽驱动背光；**与 WS2812 复用** |
| **CST816S 触摸** | **GPIO22(SCL), 23(SDA), 35(INT)** | I2C 总线 | 板载 3.3kΩ 上拉电阻；GPIO35 仅作输入 |
| **DHT11 温湿度** | **GPIO25** | 双向单总线 (JP1) | 板载 10kΩ 上拉电阻 |
| **HC-SR04 超声波** | **GPIO32(Trig), GPIO33(Echo)** | 脉冲测量 (JP2) | 原板按 3.3V 供电，测距范围 2~400cm |
| **SR602 人体红外** | **GPIO34** | 输入 (Input Only, JP5) | 仅作输入；有人输出高电平 `1`，无人输出 `0` |
| **NTC 温度采样** | **GPIO36 (VP)** | ADC 模拟输入 (JP4) | 仅作输入；板载 10kΩ/1% 精密分压电阻 |
| **WS2812 RGB 灯** | **GPIO26** | 单线 RMT 脉冲 (JP3) | **做此实验时须先拔掉 LCD 背光跳线帽** |
| **MicroSD / TF 卡** | **GPIO2, 4, 12, 13, 14, 15** | 4-bit SDIO | GPIO2/0 关联，注意不要外接强下拉 |

---

## 🚀 实战学习路线（6 阶段 · 18 关卡进阶体系）

```
[阶段一: 见光与数字IO] 第 01 关 (串口通信) ──► 第 02 关 (PWM呼吸灯) ──► 第 03 关 (按键/红外)
                                                                               │
[阶段二: 中断与操作系统] 第 06 关 (WS2812彩灯) ◄── 第 05 关 (FreeRTOS多任务) ◄── 第 04 关 (GPIO中断)
         │
[阶段三: 总线与传感器]   第 07 关 (ADC测温/超声波) ──► 第 08 关 (I2C总线/DHT11)
                                                                               │
[阶段四: 存储与视觉交互] 第 11 关 (LVGL/电容触摸) ◄── 第 10 关 (ST7789彩屏) ◄── 第 09 关 (NVS存储)
         │
[阶段五: 无线与物联网]   第 12 关 (WiFi/天气时钟) ──► 第 13 关 (MQTT云端控制) ──► 第 14 关 (BLE微信小程序)
                                                                                         │
[阶段六: 架构、扩展与结业] 第 18 关 (终极中控大作) ◄── 第 17 关 (软件工程分层) ◄── 第 16 关 (低功耗) ◄── 第 15 关 (TF卡)
```

---

### 【阶段一：见光验证与数字控制 —— 基础输入输出】

#### 第 01 关：ESP32 串口通信与 Hello World 深度剖析 📖 [【阅读深度教程】](./book/01_串口通信与HelloWorld深度解析.md)
- [x] **打卡目标**：验证电脑与 ESP32 的下载与通信链路正常。
- **涉及引脚**：Type-C (UART0 `GPIO1/3`)
- **实验内容**：
  1. 编写最简主函数，使用 `ESP_LOGI()` 每隔 1 秒在串口打印一行 `Hello ESP32!`。
  2. 烧录固件并在 VS Code 终端打开串口监视器（Monitor）查看日志。
- **核心知识**：FreeRTOS 延时 `vTaskDelay()`、ESP-IDF 日志输出宏 `ESP_LOGI`、芯片参数读取。

#### 第 02 关：ESP32 GPIO 数字输出与 PWM 呼吸灯 📖 [【阅读深度教程】](./book/02_GPIO输出与PWM呼吸灯.md)
- [x] **打卡目标**：学会单片机最基础的 GPIO 数字输出与 PWM 硬件定时器。
- **涉及引脚**：**GPIO27** (LED2 蓝色)
- **实验内容**：
  1. 初始化 GPIO27 为输出模式（Output），实现 500ms 间隔交替闪烁（Blink）。
  2. 使用 LEDC (PWM) 外设控制 LED2 实现 13 位分辨率的平滑“呼吸灯”渐变渐暗效果。
- **核心知识**：`gpio_config()`、`gpio_set_level()`、数字高低电平原理、LEDC PWM 占空比。

#### 第 03 关：ESP32 GPIO 数字输入与人体红外感应 📖 [【阅读深度教程】](./book/03_按键检测与人体红外感应.md)
- [x] **打卡目标**：学会读取外部数字信号、机械消抖与红外感应。
- **涉及引脚**：**GPIO39** (SW3 用户按键)、**GPIO34** (SR602 红外)、**GPIO27** (LED2)
- **实验内容**：
  1. 将 GPIO39 配置为输入，读取按键状态；按下按键时翻转 LED2 亮灭。
  2. 加入 20ms 软件消抖与边沿检测算法，彻底解决按键连弹误触问题。
  3. 对齐接入 SR602 人体红外探头（`S` | `VCC` | `GND`），捕获有人活动与离开状态。
- **核心知识**：`gpio_get_level()`、机械按键抖动原理、20ms 软件消抖法、纯输入管脚限制。

---

### 【阶段二：中断机制与 FreeRTOS 操作系统 —— 底层基石】

#### 第 04 关：ESP32 GPIO 外部中断(ISR)与按键事件驱动 📖 [【阅读深度教程】](./book/04_GPIO外部中断与按键事件驱动.md)
- [x] **打卡目标**：告别 `while(1)` 轮询，掌握单片机核心武器 —— 硬件中断。
- **涉及引脚**：**GPIO39** (SW3 用户按键)、**GPIO27** (LED2)
- **实验内容**：
  1. 将 GPIO39 配置为下降沿中断触发（`GPIO_INTR_NEGEDGE`）。
  2. 注册硬件中断服务函数（ISR Handler），在按键按下的微秒级瞬间打断 CPU 响应。
  3. 学习中断上下文与普通任务上下文的区别，实现极低延迟的事件驱动。
- **核心知识**：`gpio_install_isr_service()`、`gpio_isr_handler_add()`、中断服务函数（ISR）编写规范、`IRAM_ATTR` 内存修饰符。

#### 第 05 关：FreeRTOS 多任务调度与队列(Queue)跨任务通信 📖 [【阅读深度教程】](./book/05_FreeRTOS多任务调度与队列通信.md)
- [x] **打卡目标**：掌握嵌入式操作系统的灵魂 —— 多任务并行与数据安全传递。
- **涉及引脚**：**GPIO39** (按键), **GPIO34** (红外), **GPIO27** (LED2)
- **实验内容**：
  1. 创建 2 个独立并发的 FreeRTOS 任务：`Task_Sensor`（负责后台采集）与 `Task_LED`（负责执行控制）。
  2. 创建 FreeRTOS 消息队列（Queue），当中断或传感器产生事件时，将数据打包压入队列。
  3. LED 任务从队列中阻塞等待数据，收到消息后执行相应动作。
- **核心知识**：`xTaskCreate()`、`vTaskDelete()`、`xQueueCreate()`、`xQueueSendFromISR()`、`xQueueReceive()`、任务优先级与栈空间分配。

#### 第 06 关：ESP32 RMT 硬件脉冲外设与 WS2812 幻彩 RGB 跑马灯 📖 [【阅读深度教程】](./book/06_RMT硬件脉冲与WS2812幻彩RGB.md)
- [x] **打卡目标**：学习 ESP32 独门绝技 —— 纳秒级 RMT 脉冲外设，驱动单线彩虹灯。
- **涉及引脚**：**GPIO26** (JP3 WS2812 接口) —— *⚠️ 须先取下 JP7 背光跳线帽*
- **实验内容**：
  1. 学习 WS2812 单线归零码时序（800kHz 高低电平脉冲要求）。
  2. 配置 ESP-IDF RMT 驱动生成精准纳秒级波形。
  3. 实现 HSV 转 RGB 色彩转换算法，开发 10 大酷炫灯效（彩虹流光、赛博朋克双向对撞、烈焰壁炉、反应堆心跳、雷达扫描等），并支持按键 SW3 循环切换与全黑静音熄灭。
- **核心知识**：RMT（Remote Control Peripheral）外设、HSV 与 RGB 颜色空间转换、色彩渐变与多模式状态机、GPIO 复用与跳线帽排查。

---

### 【阶段三：传感器集结与通信总线 —— 模拟量与数据通信】

#### 第 07 关：ESP32 模拟量采集(ADC 测温)与超声波测距(HC-SR04) 📖 [【阅读深度教程】](./book/07_ADC模数转换与超声波测距.md)
- [x] **打卡目标**：学会采集连续变化的模拟电压与微妙级高精度脉冲测距。
- **涉及引脚**：
  - NTC 测温：**GPIO36 (ADC1_CH0, JP4)**
  - HC-SR04 超声波：**GPIO32** (Trig 发射), **GPIO33** (Echo 接收, JP2)
- **实验内容**：
  1. **NTC 测温**：配置 ADC OneShot 模式读取 GPIO36 模拟电压，结合板载 10kΩ/1% 分压电阻与 Steinhart-Hart 公式换算当前环境温度（℃）。
  2. **超声波测距**：GPIO32 发送 10µs 触发脉冲，使用 `esp_timer_get_time()` 测量 GPIO33 高电平回响时间，换算出障碍物距离（cm）。
  3. **温声融合雷达**：结合环境温度动态校准空气声速（$v = 331.3 + 0.606 \times T$），实现工业级高精度测距。
- **核心知识**：ADC 采样与衰减设置（Attenuation）、参考电压校准（ADC Calibration）、微秒级时间戳测量、声速温度动态补偿。

#### 第 08 关：I2C 通信总线探秘(I2C Scanner)与 DHT11 单总线时序解析 📖 [【阅读深度教程】](./book/08_I2C总线探秘与DHT11温湿度解析.md)
- [x] **打卡目标**：掌握芯片间主流通信语言 I2C 总线与微秒级单总线协议。
- **涉及引脚**：
  - I2C 总线：**GPIO22 (SCL)**, **GPIO23 (SDA)**
  - DHT11 温湿度：**GPIO25 (JP1)**
- **实验内容**：
  1. 初始化 I2C 主机模式，编写一个 **I2C 总线地址扫描器（I2C Scanner）**，遍历扫描并打印板载从机设备地址（发现 CST816S 触摸芯片 `0x15`）。
  2. 编写 DHT11 单总线驱动：发送 18ms 起始信号，精准捕获 40-bit 微秒级高低电平数据包并校验温度与湿度。
  3. 双任务并发：实现 I2C 触摸在线监测与 DHT11 气象监测雷达。
- **核心知识**：I2C 开漏上拉与 ACK 握手、`driver/i2c_master.h`、`i2c_master_probe()`、单总线（1-Wire）时序精确解析、数据校验和（Checksum）。

---

### 【阶段四：本地存储与视觉交互 —— 现代化人机界面】

#### 第 09 关：ESP32 NVS 非易失性存储与 Flash 偏好设置(断电不丢数据) 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：学会利用内部 Flash 存储关键配置，实现断电不丢失。
- **涉及技术**：NVS（Non-Volatile Storage）键值对存储
- **实验内容**：
  1. 初始化 NVS 分区与开机计数器持久化。
  2. 将用户自定义的“屏幕亮度偏好”、“深色模式”以及“Wi-Fi 账号密码”以键值对（Key-Value）写入 NVS。
  3. 综合大工程：设备运行时间持久化与 SW3 长按 3 秒恢复出厂设置（Factory Reset）。
- **核心知识**：Flash 记事本 vs RAM 草稿纸、命名空间抽屉、`nvs_set_xxx` / `nvs_get_xxx`、`nvs_commit()` 关键提交、`nvs_flash_erase()` 格式化。

#### 第 10 关：ESP32 驱动 1.69寸 ST7789 彩屏与几何图形渲染(SPI DMA) 📖 [【阅读深度教程】](./book/10_ST7789彩屏驱动与几何图形渲染.md)
- [x] **打卡目标**：掌握高速 SPI 屏幕初始化与 DMA 显存加速。
- **涉及引脚**：**GPIO5(CS), 17(DC), 18(SCLK), 19(MOSI), 21(RST)** + **GPIO26(背光 BL)**
- **实验内容**：
  1. 初始化 SPI 总线与 ST7789 驱动，开启 DMA 内存加速通道与反色使能。
  2. 点亮背光，执行全屏纯色刷屏（红、绿、蓝、黄、青、白）。
  3. 掌握“红点试探法”与原点物理视口偏移（Gap: X=20, Y=0）校准。
  4. 绘制基本几何图形：点、线、实心矩形、实心圆、同心圆与科技感卡片 UI。
  5. 综合大工程：局部 DMA 刷新动态正弦波示波器与 50+ FPS 实时帧率平滑刷新。
- **核心知识**：RGB565 颜色编码原理、DMA 顺丰快递员硬件加速机制、ST7789 Gap(20,0) 物理对齐、常驻 line_buffer 消除毛刺、局部显存无损重绘。

#### 第 11 关：ESP32 搭载 LVGL v9 现代图形界面与 CST816S 电容触摸实战 📖 [【阅读深度教程】](./book/11_LVGL图形框架与电容触摸实战.md)
- [x] **打卡目标**：跑通现代嵌入式 GUI 框架，实现手指触摸交互。
- **涉及引脚**：ST7789 屏幕 SPI 引脚 + **GPIO22(SCL), 23(SDA), 35(INT)** (CST816S 触摸) + **GPIO27(LED2)**
- **实验内容**：
  1. 移植 **LVGL v9** 图形库，使用 `esp_lvgl_port` 官方端口管理 FreeRTOS 渲染任务。
  2. 初始化 CST816S I2C 触摸芯片并注册为 LVGL 输入设备（Pointer Device）。
  3. 搭建科技感卡片 UI：流光加载环（Spinner）、交互按钮（Button）。
  4. 综合大工程：智能家居触控中控屏（Arc 仪表盘、Switch 开关、Slider 亮度调节）。
- **核心知识**：面向对象对象树（Object Tree）、对齐九宫格、样式选择器、`esp_lvgl_port` 互斥锁机制、CST816S 电容微观感应原理、事件驱动模型（Event-Driven）。

---

### 【阶段五：无线互联与智能物联网 —— 打通手机与云端】

#### 第 12 关：ESP32 Wi-Fi 联网、SNTP 网络授时时钟与 HTTP/HTTPS 动态天气 📖 [【阅读深度教程】](./book/12_WiFi连接管理与HTTP天气时钟.md)
- [x] **打卡目标**：让 ESP32 融入局域网与互联网，自动校准时间、IP 地理定位并安全拉取加密天气。
- **涉及模块**：Wi-Fi STA 模式 + FreeRTOS 事件组 + SNTP 网络时钟 + ESP-HTTP-Client + MbedTLS 全球根证书包 + cJSON
- **实验内容**：
  1. **Wi-Fi 客户端连接**：配置 STA 模式，理解 `esp_netif_init` / `esp_event_loop_create_default` 底层架构，利用 FreeRTOS 事件组 `xEventGroupWaitBits` 实现 0% CPU 阻塞等待与有限重连。
  2. **SNTP 网络授时**：连接阿里云 NTP 服务器（`ntp.aliyun.com`），设置 `TZ=CST-8` 东八区时区并阻塞等待硬件时间同步。
  3. **IP 地理定位 + 动态天气时钟**：两段式级联流水线 —— 先向 `http://ip-api.com/json/` 探测公网 IP 获取所在城市与经纬度，再动态拼接 URL 轮询当地实时天气。
  4. **HTTPS 证书加密通信**：引入 `esp_crt_bundle_attach` 挂载全球权威 CA 根证书包，掌握 TLS 握手原理，安全请求 HTTPS 加密天气 API。
- **核心知识**：Wi-Fi 初始化七步曲流水线、FreeRTOS 事件组（五大参数拆解）、SNTP 授时与时区、两段式 IP 定位架构、TLS 握手四步曲、CA 根证书包自动校验。

#### 第 13 关：ESP32 MQTT 物联网双向通信与云平台联动实战(手机远程控制) 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：掌握物联网行业标准通信协议 MQTT，实现手机远程双向控制。
- **涉及协议**：MQTT 协议 + EMQX / 阿里云 IoT / Home Assistant
- **实验内容**：
  1. 连接公共 Broker（broker.emqx.io），订阅测试主题实现消息自发自收。
  2. 构造并定时上报 JSON 格式设备遥测数据（可用内存、系统运行时间、实时气温）。
  3. 综合大工程：手机端 App / MQTTX 发送 JSON 指令，远程控制开发板 LED2 并实时回传 ACK。
- **核心知识**：MQTT 发布/订阅模型（Pub/Sub）、QoS 服务质量、微信公众号生动比喻、断线自动重连。

#### 第 14 关：ESP32 BLE 低功耗蓝牙实战与微信小程序双向互联 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：掌握低功耗蓝牙通信，用手机微信小程序/调试 App 免配网近场控制开发板。
- **涉及协议**：BLE GATT 协议栈（Bluedroid）+ 手机蓝牙 App / 微信小程序
- **实验内容**：
  1. 配置 ESP32 为 BLE 外设，广播自定义设备名称（`ESP32-Journey-Beacon`）。
  2. 搭建 GATT Server，创建自定义 Service 与读写特征值（`0x00FF` / `0xFF01`）。
  3. 综合大工程：手机写入控制板载 LED2，板载按键 SW3 触发主动 Notify 弹射推送至手机。
- **核心知识**：BLE 广播包格式（Advertising）、GATT 树状架构（Service / Characteristic）、UUID 规则、手机端蓝牙交互流程。

---

### 【阶段六：存储扩展、低功耗与毕业设计实战】

#### 第 15 关：ESP32 挂载 MicroSD/TF 卡(4-bit SDIO)与 FATFS 电子相册 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：利用板载 TF 卡槽读取外部大容量文件资源。
- **涉及引脚**：**GPIO2, 4, 12, 13, 14, 15** (4-bit SDIO)
- **实验内容**：
  1. 通过 SDMMC 硬件外设以高速总线挂载 FATFS 虚拟文件系统（`/sdcard`）。
  2. 构造传感器数据黑匣子，周期性将温度与内存状态按 CSV 格式追加写入文件并 `fflush` 刷盘。
  3. 综合大工程：使用 POSIX `opendir / readdir` 递归扫描整张 TF 卡文件目录树与文件大小。
- **核心知识**：4-bit SDIO 高速传输协议、FATFS 虚拟文件系统接口（VFS）、标准 C 文件流 `fopen / fread / fflush`、目录树递归遍历。

#### 第 16 关：ESP32 低功耗电源管理与 Deep-sleep 休眠唤醒(电池省电技术) 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：掌握电池供电核心技术，将整机待机功耗降低至微安级。
- **涉及技术**：ESP32 电源管理 + Deep-sleep 深度睡眠 + EXT0/EXT1 外部唤醒
- **实验内容**：
  1. 配置 Timer 定时器深度睡眠，并使用 `RTC_DATA_ATTR` 实现断电保持开机计数。
  2. 配置外部唤醒源：通过按键 SW3（`GPIO39`, EXT0）或 SR602 红外传感器（`GPIO34`, EXT1）瞬间将芯片唤醒。
  3. 综合大工程：低功耗环境监测哨兵，平时微安级沉睡，定时/报警瞬间唤醒巡检，处理完毕重新秒级入睡。
- **核心知识**：RTC 控制器、RTC GPIO 唤醒配置、掉电保持变量（`RTC_DATA_ATTR`）、EXT0/EXT1 外部唤醒。

#### 第 17 关：嵌入式软件工程化 —— 驱动/业务分层、事件总线与组件化模块设计 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：告别“千行代码全堆在 `app_main.c`”的意大利面条式写法，建立企业级三层分层架构思维。
- **涉及核心架构思想**：
  1. **三层经典分层架构（BSP驱动层 / 中间件服务层 / 业务逻辑与UI层）**：后厨换传感器，前厅UI一行不改。
  2. **统一事件数据总线（ESP Event Loop / 消息订阅）**：彻底解耦传感器采集与屏幕渲染，杜绝跨模块直接调用的代码泥潭。
  3. **有限状态机（FSM, Finite State Machine）**：用状态机统一管理自检、待机、活跃与报警，消灭多层 `if-else` 嵌套。
- **核心知识**：高内聚低耦合设计原则、事件驱动架构、状态机转移表、面向对象语义化 API 封装。

#### 第 18 关：ESP32 终极综合大实战 —— 桌面多功能智能气象站与物联网超级中控台 📝 *[大纲规划 / 连载中]*
- [ ] **打卡目标**：融会贯通前面所有 17 关的软硬件技能与分层工程架构，完成一个功能完整、代码优雅、商业级的独立大作！
- **完整功能矩阵**：
  1. **工业级分层架构**：按 BSP驱动 / 系统服务 / UI业务 严格模块化分层解耦，代码极具扩展性与工程美感。
  2. **多任务并行调度**：FreeRTOS 调度 4 个核心任务（UI 渲染任务、传感器采集任务、网络与 MQTT 任务、电源管理任务）。
  3. **华丽 LVGL v9 触控交互**：炫酷平滑滑动多页面（表盘时钟页、室内外温湿度折线曲线页、WS2812 幻彩灯控页、系统状态页）。
  4. **全方位数据融合**：Wi-Fi 自动 SNTP 对时 + HTTP 解析实时天气预报 + 板载 DHT11/NTC/超声波实时数据，历史曲线存入 TF 卡。
  5. **双模智能互联**：MQTT 接入智能家居手机远程遥控 + 微信小程序 BLE 蓝牙无网快速配网与调试。
  6. **智能节能感应**：SR602 人体红外感知无人时 30 秒自动休眠息屏，有人靠近瞬间硬件中断唤醒亮屏。
- **核心知识**：大型嵌入式工程架构设计、复杂状态机、跨模块解耦、内存与性能终极调优。

---

## 🛠️ 小白调试避坑黄金法则

1. **串口日志是你的第一双眼睛**：
   - 遇到任何异常或重启，终端里会输出红色的 `Guru Meditation Error` 或堆栈崩溃信息。
2. **硬件复用陷阱**：
   - **GPIO26** 默认接 LCD 背光，做 WS2812 RGB 灯环实验时，**必须取下 JP7 背光跳线帽**。
3. **只读引脚不可输出**：
   - `GPIO34`、`GPIO35`、`GPIO36`、`GPIO39` 为纯输入管脚，切勿在代码中配置为 `GPIO_MODE_OUTPUT`。
4. **内存管理好习惯**：
   - 复杂图形或大数组优先分配在 **2MB PSRAM**（使用 `heap_caps_malloc(size, MALLOC_CAP_SPIRAM)`），保留宝贵的内部 SRAM 给 Wi-Fi 和协议栈。
